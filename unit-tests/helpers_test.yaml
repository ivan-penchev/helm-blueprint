suite: Helper templates
templates:
  - templates/workloads/deployment.yaml
set:
  containers:
    app:
      image:
        repository: nginx
      ports:
        http:
          port: 80
  networking:
    services:
      http:
        ports:
          http:
            port: 80
tests:
  - it: should set default fullname to RELEASE-NAME-helm-blueprint
    asserts:
      - equal:
          path: metadata.name
          value: RELEASE-NAME-helm-blueprint

  - it: should use nameOverride in the name
    set:
      nameOverride: myapp
    asserts:
      - equal:
          path: metadata.name
          value: RELEASE-NAME-myapp

  - it: should use fullnameOverride as entire name
    set:
      fullnameOverride: custom-name
    asserts:
      - equal:
          path: metadata.name
          value: custom-name
