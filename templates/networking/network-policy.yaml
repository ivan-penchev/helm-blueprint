{{- $fullName := include "helm-blueprint.fullname" . -}}
{{- range $name, $np := .Values.networking.networkPolicies }}
{{- if $np }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
{{ include "helm-blueprint.metadata" (dict "name" (printf "%s-%s" $fullName $name) "context" $ "extraAnnotations" $np.annotations "extraLabels" $np.labels "syncWaveDefault" "-1" "syncWave" $np.syncWave) }}
spec:
  {{- if $np.podSelector }}
  podSelector:
    {{- toYaml $np.podSelector | nindent 4 }}
  {{- else }}
  podSelector:
    matchLabels:
      {{- include "helm-blueprint.selectorLabels" $ | nindent 6 }}
  {{- end }}
  {{- with $np.policyTypes }}
  policyTypes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $np.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $np.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
